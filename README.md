# Automation2025

Not in onedrive, in E: 

`py -m venv venv` to create venv
view-> cmd palette, Python: Select Interpreter to slect venv

First time: 
    By default scriptrunning disabled. to enable for locally created scripts:
    `Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser`
